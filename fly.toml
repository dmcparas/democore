app = "democore"
primary_region = "lhr"

[build]
dockerfile = "Dockerfile"

[[services]]
internal_port = 9090
protocol = "tcp"

[[services.ports]]
port = 80   # external port for plaintext gRPC
# NO handlers needed → raw TCP

[[services.tcp_checks]]
interval = "15s"
timeout = "2s"
grace_period = "10s"